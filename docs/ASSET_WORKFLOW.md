# Asset Workflow

Identify exact source → extract/reconstruct deterministically → preserve native identity/hashes → create reviewable representation if useful → convert only for a defined target → deduplicate only by byte/hash evidence → register manifest → round-trip validate → publish in small reviewable batches.

Cross-generation output must retain both source identity and target contract.