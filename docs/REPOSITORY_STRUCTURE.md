# Repository Structure

Stable purpose-based areas are created only when real material exists: `docs/`, `manifests/`, `src/`, `data/`, `assets/`, `tools/`, `tests/`, `patches/`. Verified platforms may extend this locally. Do not create repository-wide structure-version roots, migration-history trees, or duplicated canonical homes.