# Thumb bootstrap call targets

The verified entry CFG contains 21 direct BL sites and 19 unique in-ROM targets. Target `0x08000348` is called three times; all other targets occur once. These addresses are reconstruction candidates, not inferred function names. Callee traversal remains a separate verified step.
